set -g history-limit 50000
set -g status off
set -g mouse off

# 把 tmux window 标题打穿到终端标题
set-option -g set-titles on
set-option -g set-titles-string "#W"

# 多客户端协作：窗口尺寸跟随最后操作的客户端（collab.md §2）
set -g window-size latest
